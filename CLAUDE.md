# Harness 项目规范

## 目标与范围

实现使用 DeepSeek 的命令行查询引擎：接收用户输入，循环处理模型回复和工具调用，直到得到最终回答；逐次记录 API token 用量，提供 `/cost` 查询本次会话的用量和预估费用。

工具通过统一描述和注册表接入查询循环，开放只读的 `read_file` 和 `grep`、默认确认后执行且可按目录授权的 `write_file`、按命令风险决定是否确认的 `bash`，以及启动独立子查询的 `delegate`。

## 技术与目录约定

- 使用 Python 3.11 或更新版本和标准库，以 `tomllib` 解析 TOML，当前阶段不引入第三方运行依赖。
- `pyproject.toml` 是项目元信息及非密钥运行配置的唯一默认值来源，涵盖模型、显示、请求与重试、上下文、工具限制、费用与权限。`harness/config.py` 严格校验并缓存启动配置，文件位置固定在 Harness 项目根目录，不跟随被操作的工作目录；配置缺失或无效时明确报错，不静默放宽权限。密钥继续由 `.env` 或环境变量提供。
- `harness/permissions.py` 统一执行 `allow`、`ask`、`deny` 决策。`PermissionRule` 提供精确工具名、`action`、整数 `priority`（默认 0），以及可选 `directory` 或 `command_pattern`；配置放在 `tool.harness.permissions.rules`。工具级 `deny` 先拒绝，规则列表按禁止规则优先、数值优先级降序、同级声明顺序排列；`check_permission` 逐条匹配，第一条命中即返回，没有命中则沿用工具级配置及风险等级默认策略。未配置的新工具默认询问。
- `PermissionPolicy` 增加会话模式 `ask`／`auto` 和 `auto_directories`。`auto` 模式信任当前工作目录或指定相对目录，自动放行读取、写入、验证和常见 Node/Python/Perl 脚本；`deny` 始终优先，网络、破坏性命令、进程管理、敏感路径、越界和重定向仍须确认。
- 目录规则仅用于单文件操作 `read_file`、`write_file`，以启动工作目录为根，支持目录及全部子目录；配置目录必须是无 `..` 的相对路径。放行要求标准化路径与解析软链接后的实际路径都在指定目录内，询问／禁止同时比较候选路径及配置目录各自的词法和实际路径，任一组合命中即生效，避免别名绕过限制；按路径组件判断，匹配异常不得放行。`grep` 仅支持工具级规则，不将搜索起点检查冒充搜索结果目录隔离。命令规则仅用于 `bash`，用正则搜索完整命令原文，启动时验证正则，不宣称能够识别任意 Shell 等价写法或替代沙箱。
- 默认读文件与搜索放行，写文件询问；Bash 根据启发式风险兜底，仅明确识别的简单只读命令可放行，其他命令至少询问。工具名 `allow` 不能免除写入或危险 Bash 确认；目录规则和已确认的会话目录可授权写入。Bash 不接受强制 `allow` 规则，工具名 `ask` 可强制所有命令询问；禁止始终优先。规则启动时加载为不可变快照，默认 `rules = []`，示例只在文档中提供，配置错误停止启动。
- 风险与界面保持一致：读取低风险直接执行，写入与未识别命令中风险确认，破坏性命令高风险醒目警告后确认。`harness/bash_risk.py` 实现 `classify_bash_risk`（从 `permissions.py` 导出），返回 `read_only`／`write`／`destructive`；检查危险关键词、下载并执行管道、敏感路径、组合命令及环境修改。只读白名单涵盖 `pwd`、`ls`、`cat`、`head`、`tail`、受限 `sed -n`、`grep`、`find`、`wc`、`echo`、`printf`、`cd` 和仅版本参数的 `node`／`python*`，可组合 `&&`、`;` 及 `2>/dev/null`；任意解释器脚本、重定向、进程管理、`sed -i`、`find -delete/-exec` 等仍须确认。只读执行须固定系统 PATH、清除导入函数和动态加载环境，分类不代表完整 Shell 解析或沙箱。
- 新工具默认中风险询问；显式 `deny` 直接拒绝，不得通过警告或会话记忆覆盖。警告不依赖终端颜色，需要确认时仅 `y` 批准；回车／拒绝／EOF／取消均不执行。
- `SessionPermissionCache` 仅记忆当前执行器会话中经用户明确确认并成功写入的父目录及子目录，CLI 确认时必须展示授权目录范围；禁止规则先于缓存。参数或目标路径在确认期间变化、失败、拒绝及非交互不得新增授权；不同执行器不共享缓存，重启失效，Bash 不记忆。对直接嵌入的执行器，只有显式提供会话缓存才启用记忆，避免把旧单次确认回调扩大为目录授权。
- `harness/audit.py` 负责 `.harness/permission.log` 的 JSONL 审计，记录 UTC 时间、会话／调用标识、工具、脱敏参数、风险、匹配规则、决策和确认状态；正文、搜索关键词和原始命令一律不记录，仅保留路径和必要长度／指纹。权限与确认结果须在工具副作用前落盘，日志错误拒绝执行。日志目录和文件拒绝软链接／特殊文件，私有权限创建，`.harness/` 排除 Git；测试日志仅写临时工作区。
- `.harness` 属于内部运行目录，读写与搜索工具按既有敏感路径机制拒绝访问，防止修改日志或将日志混入搜索结果；用户可在终端自行查看。可选规则 `name` 用于拒绝说明与审计，未提供时使用 TOML 声明位置 `rules[index]`，名称必须唯一且可显示。
- `harness/`：命令行入口、DeepSeek 客户端、查询循环、工具注册与分发、用量统计。
- `harness/tools/`：统一容纳三层工具系统。`definition.py` 定义工具名称、说明、参数及内部 `supports_cancellation` 标志（默认 `False`），并转换为 DeepSeek 格式；`registry.py` 自动发现工具模块、注册和按名称查找；`executor.py` 校验参数、检查统一权限、要求必要的本地确认、分发执行并统一结果与错误；`__init__.py` 初始化并导出公共接口。内部能力不作为模型可填写的参数。
- 具体工具独立成文件，如 `harness/tools/read_file.py`，提供 `DEFINITION` 与 `execute(arguments, workspace)`。启动时自动发现该目录直属模块，跳过基础模块和下划线开头的辅助模块；重复名称、缺失定义或不可调用的实现必须明确报错。新增工具无需改注册列表或查询循环。
- 声明 `supports_cancellation=True` 的长运行工具额外接受关键字参数 `abort=None`，执行层传入会话取消事件；现有读写工具保持两参数接口。工具返回失败状态时执行层不得覆盖为成功，已启动的命令失败仍记为 `executed=True`。
- 核心入口为 `harness/engine.py` 的 `query_loop(state)`，保持「请求模型、识别工具调用、执行、回传、继续」的骨架清晰。
- `harness/orchestration.py` 负责子任务编排，`harness/tools/delegate.py` 沿用自动注册。模型仅填写 `description`（短标题）和 `task`（完整任务说明），不接受客户端、权限或批准参数。系统提示要求目录级、多文件、代码质量、跨文件对比等需要读取大量文件的任务优先委托，不在主会话逐文件读取；子 Agent 继续沿用独立任务规则，工具描述和执行注册表均排除 `delegate`。执行器在既有权限与审计守卫之后注入内部运行回调；查询循环通过标准库 `ContextVar` 在工具调用期间绑定当前查询，退出调用时恢复，避免全局会话混用和循环导入。
- 子 Agent 同步完成后返回报告，使用父查询的 DeepSeek 客户端与模型，但创建只含固定系统提示和子任务的独立 `messages`，不自动复制主历史；子回答作为带原调用标识的工具结果回传主 AI。子模型工具描述和执行注册表均不含 `delegate`，执行层不得仅隐藏说明而允许递归委托；只继承父查询已开放的现有工具。
- 子任务继承父执行器的工作区、不可变权限快照、确认入口、会话目录授权和审计，并共用父查询取消事件；不得重新创建默认权限。默认允许 `delegate`，可配置询问或禁止，子工具仍逐次经过既有权限检查。
- 主、子请求（含重试与摘要）共用本次主查询的总请求上限，子查询通过内部 `request_parent` 将实际请求计入父计数，并保留至少一次请求额度供主 AI 整理结果；`/cost` 使用同一账本，不重复记账。每个子查询仍使用现有上下文、重试和工具结果限制。CLI 显示 `[delegate]` 启动、完成／失败，子回答流不直接混入主回答，用量与工具事件可显示子任务标识。
- 主、子查询连续读取页面时，按完整用户轮次无法释放当前轮，允许摘要当前轮更早的已完成工具批次；保留系统提示、最近完整用户轮次、原任务和最后一组完整 assistant／tool 批次，不拆分工具调用与结果，未完成或异常工具链不得压缩。已有阶段摘要可以再次压缩，仍沿用每次提问六次压缩上限、候选验证与失败回滚。子上下文或请求额度最终耗尽后，本次主提问不再启动子查询，主 AI 不得转回主会话执行原任务；下一次用户提问重新允许委托。
- `harness/background.py` 提供会话级 `BackgroundManager`，管理 `PENDING`、`RUNNING`、`COMPLETED`、`FAILED` 状态、FIFO 排队、最多 5 个并发和默认 300 秒超时。任务结果保存在当前进程内存，跨用户轮次可查询；`/exit` 或输入结束时停止运行中和排队的任务，daemon 线程不阻止退出。超时通过任务停止事件协作取消，超时先标记 `FAILED`，任务稍后返回不得覆盖最终状态或重复发送完成事件。
- 自动注册 `background_submit(description, command|task, timeout)` 与 `background_check(task_id)`。后台 Bash 命令由 `background_submit` 权限决策控制，但仍调用 Bash 规则检查：`deny` 优先，非只读命令不能因工具级 `allow` 跳过确认，实际执行沿用前台 Bash 最多 120 秒限制。后台分析创建独立查询，不复制主历史，继承主查询可见工具但排除委托和后台工具，并使用独立请求预算和同一用量账本。
- 后台运行入口仅由执行器在权限与审计守卫之后注入，模型不能传入函数、管理器或运行回调。子 Agent 工具注册表同样排除 `background_submit` 和 `background_check`，避免递归或嵌套后台任务。
- `harness/tools/swarm.py` 自动注册团队协作工具。每个角色由 `SwarmRole(name, system, tools, handoff_to)` 定义，角色上下文独立，只接收 `Handoff` 中的总任务、摘要、产物路径和打回反馈；编排器串行运行角色查询，解析最终 JSON 交接，验证目标存在且位于允许交接列表，`max_rounds` 默认 10 防止 Reviewer 与 Coder 无限回退。
- `harness/tools/run_verify.py` 运行工作区内明确存在的 Node／Python 验证脚本，不接受内联代码或任意 shell 参数；目标必须位于会话工作区并排除敏感路径，运行方式允许 `node`、`python3`、`unittest`、`node-test`、`npm-test`。该工具默认中风险确认，工具级 `allow` 不能跳过首次确认；批准后当前会话内同目录及子目录的后续验证复用授权，CLI 只展示简短目标和运行方式。
- 默认 Swarm 团队为 Coder、Reviewer、Tester。角色工具必须来自主查询当前可见工具，且排除 `delegate`、`background_submit`、`background_check` 和 `swarm`；写文件和 Bash 仍逐次经过权限与确认。角色失败、非法交接、未知目标、请求耗尽或达到轮次上限返回安全错误，不把角色模型原文直接交给主 AI。
- Swarm 使用独立于主查询的 `RequestBudget`：默认团队总请求上限 120 次、单角色上限 40 次，角色请求不消耗主查询自己的 20 次额度。角色达到单角色上限返回 `swarm_role_request_limit`，团队总预算耗尽返回 `swarm_request_limit`。
- CLI 对 Swarm 角色内部工具、请求用量、压缩和重试事件默认静默，只显示角色开始、完成、交接和团队结果。
- 任何包含 `bash` 的 Swarm 角色自动获得 `run_verify`，角色提示优先使用它运行已有测试，减少重复 Bash 确认。
- CLI 显示 `[swarm] 角色分配`、角色开始／完成、交接目标和团队完成／失败；角色回答流不直接混入主输出，用量和工具事件带 `[swarm]` 标识。
- `tests/`：使用 `unittest` 验证行为，默认不访问真实 DeepSeek API。
- `README.md`：启动方法、配置、行为和限制。
- `ROADMAP.md`：当前阶段、完成项、待办、阻塞和最近验证。
- `CHANGELOG.md`：按版本记录已验证的功能和已知限制。
- `docs/diagrams/`：技术说明图，保存可编辑 SVG、导出的 PNG 和离线 HTML 预览；同名文件表示同一张图，内容须与当前实现核对，目录 README 记录使用和结构约定。

## 实现要求

- 以下运行数值为 `pyproject.toml` 的初始默认值；后续配置修改必须同时作用于实际执行和工具说明／参数范围，不能只改显示。配置中的字段、类型、有限数值、上下界及互相关系须验证；API key 不得配置到 TOML。配置加载一次，同一进程内修改文件不改变当前权限。
- 权限判断在参数校验后、确认和工具执行前完成；`deny` 返回 `permission_denied`，不得询问后放行；`ask` 保留逐次确认、取消、参数快照及无确认入口拒绝规则。CLI 对读写／Bash 以外需要确认的工具展示工具名和参数 JSON，支持新工具默认询问。
- 工具执行前、最后一次审计落盘后复核权限决策和单文件实际目标，避免等待确认或日志写入时路径变化后落入禁止目录；命中禁止或检查异常时拒绝，其他决策或目标变化要求重新发起调用，不沿用旧批准。此检查不提供抵御并发文件系统改写的原子沙箱保证。
- 目录放行按路径拼写精确匹配，不扩大授权；目录询问／禁止以 Unicode NFC 规范化并折叠大小写的路径保守匹配，覆盖 macOS 大小写及 Unicode 等价别名，也覆盖尚未创建的目录。在区分大小写的文件系统中，询问／禁止会同时限制仅大小写或 Unicode 规范形式不同的目录。
- DeepSeek 请求和计费字段以官方文档为准；记录费率来源及核验日期。
- 启动时优先读取 `DEEPSEEK_API_KEY` 环境变量，未设置时从项目根目录 `.env` 读取同名键；仅使用标准库，不执行文件内容，不输出密钥或请求认证信息。
- `.env` 由 `.gitignore` 排除；创建或修改实际 `.env` 文件前仍须用户明确确认。只准备空值模板，真实密钥由用户在本地填写。
- 工具调用必须保留 assistant 消息，并用对应 `tool_call_id` 回传结果。
- 工具定义使用 `name`、`description`、`input_schema`；发送给 DeepSeek 时转换为 `type: function` 与 `function.parameters`。执行层集中处理本阶段使用的对象、字符串、整数类型，以及必填、额外字段、字符串最短长度和整数上下界校验，不引入第三方依赖。
- `read_file` 必填非空字符串 `path`，可选整数 `offset`（行号从 0 开始，默认 0）、`column`（起始行内字符位置，从 0 开始，默认 0）与 `limit`（必须为正，默认每页最多 200 行）。读取 UTF-8 普通文件，保留原始换行；整文件仍受 1 MiB 上限约束。每页完整 JSON 连同执行层字段不超过当前查询的工具结果预算，返回原始位置、`next_offset`、`next_column`、`eof` 和总行数。普通页优先按整行切分，超长单行按字符续读，不使用首尾截断丢掉中间内容；空文件和超过末尾返回空内容及完成标记，非法行内位置明确报错。
- 完整文件分析由 AI 在现有查询循环中按返回游标连续调用 `read_file`，直到 `eof`；只需要指定行段时不得强制读完整文件。每一页仍经过相同参数校验、权限、审计和取消检查，不在工具内部隐藏执行整文件／模型循环。引擎仅缓存每个文件的最新分页位置，旧正文摘要后可按行号重新读取；资源耗尽时反馈未读完文件的下一页位置，不声称全文分析已完成。
- 文件读写工具的相对路径基于会话启动目录，绝对路径必须在同一目录内。解析软链接后再次检查范围，拒绝 `.env*`、`.git` 及越界路径；路径语义、文件类型和文件操作异常由具体工具处理。
- 读文件前检查类型和大小，最多读取 1 MiB，超限返回可理解的错误；读文件工具提前按结果预算分页，引擎保留统一结果上限守卫。成功返回页面内容及进度，失败返回错误代码与说明，不暴露原始异常；CLI 仅显示简短状态。
- `harness/tools/grep.py` 自动注册只读搜索工具 `grep`，无需确认。必填 `keyword` 为区分大小写的字面关键词（不是正则表达式，不接受空字符串、换行或空字符）；可选 `path`（默认 `.`，目录递归或单个文件）、`glob`（默认 `*`，文件名模式，如 `*.py`）和 `max_results`（默认 100，整数范围 1～500）。
- 搜索每层按目录、文件名排序，返回相对路径 `path`、从 1 开始的 `line_number` 和去掉行尾换行符的 `content`。无匹配返回空列表；结果达到数量或长度上限时停止收集并设置 `truncated`。超长行保留关键词附近的片段及完整关键词，标记 `line_truncated`；关键词本身超出预留结果预算时明确要求缩短。整个结果含执行层字段后不超过默认 6000 字符，保留完整的结果条目。
- 搜索沿用读文件的工作目录、敏感路径、UTF-8 普通文本及单文件 1 MiB 限制；递归不跟随目录软链接，文件软链接须解析并检查范围与敏感路径。无法读取、二进制、无效编码或过大文件跳过并统计，不使整个搜索崩溃；指定搜索根路径非法时返回工具错误。声明支持取消，扫描中检查会话取消事件。
- `write_file` 与读文件使用相同的自动注册约定，必填 `path` 和 `content`，写入 UTF-8 文本（允许空内容），最大 1 MiB，沿用工作目录、敏感路径和普通文件限制。确认后自动创建缺失的父目录，已有文件覆盖全文；失败回传工具错误。
- 写工具执行前由执行层检查规则；命中目录放行或已确认会话授权才可免确认，其他写入调用 CLI 提供的本地确认回调，展示目标路径、完整内容、新建／覆盖提示及本会话授权范围。需要确认时仅明确输入 `y` 才批准，拒绝、空输入、EOF 或退出不得写入或新增授权。不接受模型提供的批准字段，需要确认但缺少回调时拒绝。
- 收到批准后重新核对解析后的目标路径及文件是否存在；若与预览时不同，本次确认失效，拒绝写入并说明原因，避免把新建确认用于覆盖后来出现的文件。
- CLI 主线程是唯一输入读取者，后台等待确认时不得持有输出锁；`/cost`、`/help` 和 `/exit` 仍可使用。EOF 与退出必须唤醒待确认请求，关闭输入后到达的新确认请求立即拒绝；执行前再次检查取消状态。管道输入不能代替用户确认，但可以执行规则已放行的工具。
- `harness/tools/bash.py` 使用相同约定自动注册为 `bash`。必填非空 `command`，可选整数 `timeout`（秒），默认 30、范围 1～120。通过 `/bin/bash` 从会话启动目录执行，不读 shell 启动脚本，不继承 Harness 的 `DEEPSEEK_API_KEY`，标准输入关闭；当前支持 macOS／Linux，不支持交互式终端程序。
- 需要确认的命令逐次展示完整内容、工作目录、超时和实际风险，沿用 `y` 批准和拒绝／EOF／非交互不执行规则；明确识别的只读命令可直接执行。命令具有当前用户权限，工作目录不是文件访问沙箱；不宣称 bash 具备读写工具的路径隔离。
- 命令使用独立进程组，超时或会话取消时终止同组进程并限时回收；分别持续读取 stdout、stderr，限量保留每路输出（最多 64 KiB，超限保留首尾并标记），避免管道堵塞或无界内存增长。退出前为命令取消清理留出有限等待，不无限等待模型请求。
- 已启动命令的结果必须包含 `stdout`、`stderr`、实际 `exit_code`、`timed_out` 和 `cancelled`，非零退出、超时及取消为失败且保留已收集的输出；无法取得退出码时为 `null`，不得伪造成功。启动失败及参数错误按工具错误回传，不暴露原始异常。
- 模型收到工具错误后可修正参数继续调用；未知工具和无效参数同样作为工具结果回传，不使查询循环崩溃。
- 回答使用 DeepSeek 流式接口；终端按每字符约 20 毫秒显示，等待可取消且不占用输出锁，管道输出不人为限速。工具调用先完整拼接再执行，用量仍按每次请求结束时的 usage 记录，不重复打印完整回答。
- 请求上下文（messages 和 tools 序列化）默认限 64000 字符。超限时用模型摘要旧对话，保留最近 4 个完整用户轮次；当前轮工具结果撑满预算时摘要更早完整批次，保留原任务和最新完整批次。摘要最多 2000 字符，完整回复超过该上限时截到预算内；`finish_reason=length`、空回复或工具调用仍视为无效摘要，验证确实缩短后才原子替换历史。
- 每次用户提问或手动 `/compact` 最多压缩 6 次，保留部分本身超限或压缩无效时提示「太长了，建议开个新会话」。摘要失败保留原历史，不把摘要请求递归送入查询循环。
- 工具结果序列化后默认最多 12000 字符；文件内容分页返回，其他超长结果保留首尾并标注截断与原始长度，不能拆散工具调用和结果的对应关系。搜索自身的默认结果预算仍为 5500 字符。
- 命令长结果对 `stdout` 和 `stderr` 分别保留首尾，始终保留状态、退出码、超时／取消和截断标记，最终仍按 JSON 序列化后的字符数限长。
- 暂时性网络错误、超时、限流与服务错误最多重试 3 次，等待 1、2、4 秒。永久错误不重试；部分输出后重试明确标注重新生成，不能拼接两份回答或重复执行未完成的工具调用。
- 摘要与每次重试都计入用量记录和单次操作最多 20 次实际模型请求的上限；未知用量不能记为免费。`/compact` 仅在空闲时执行，过程中 `/cost` 和 `/exit` 可用。
- `/cost` 在本地处理，不触发模型请求；按每次模型请求记录用量，计入工具循环中的请求。
- 显示金额为按记录费率计算的预估值；缺失用量或缓存字段时必须说明，不能冒充准确账单。
- 对 API 错误、截断、工具参数错误和循环上限给出可理解的反馈。
- 修改保持简单、聚焦；测试通过后才能更新 ROADMAP 的完成状态。

## 验证

- 使用 `python3 -m unittest discover -s tests -v` 验证直接回答、工具循环、连续对话、异常恢复、token 计费与命令行命令。
- 未配置 API 密钥时，不宣称已通过真实接口验证。
- 本项目使用独立 Git 仓库，不修改父级仓库中本项目之外的文件，不携带父级仓库历史。
- 发布目标为 GitHub 的 `harness` 仓库；推送、远端配置、标签和 Release 操作须经用户明确确认，发布文件不得包含 `.env`、密钥或本地缓存。
