# HARNESS 项目笔记

## 技术栈

## 编码约定

## 架构决定

## 已知问题

## 当前进展

## birthday.html 需求说明（产品）

- 目标：单文件离线生日祝福页，为「德林」和「乔娜」共同庆生。
- 受众：两位寿星本人 + 转发祝福的亲友，多为手机打开。
- 必备内容模块：
  1. 标题含「生日快乐」字样；
  2. 姓名必须同时出现「德林」「乔娜」，视觉权重对等，不偏袒任何一人；
  3. 两段个性化祝福语（各自一段，正式友好中文，无错别字）；
  4. 可自定义署名占位，例如「永远爱你们的大家」。
- 交互点（至少一个，需明确反馈）：点击蛋糕吹蜡烛（烛光熄灭 + 反馈文案）、放彩纸/烟花按钮。
- 视觉氛围：气球、彩带、蛋糕、烛光、星星等，纯 CSS 或 Canvas 绘制，禁止外链图片/字体/CDN。
- 约束：单一 HTML 文件，CSS/JS 内嵌，双击可离线运行，375px 宽不横向溢出、文字不遮挡，无 console 报错，CSS/JS 分区注释。
- 验收标准：文案齐全且姓名无遗漏；离线打开无白屏；交互点击有可见反馈；控制台无报错；375px 视口无横向滚动。
