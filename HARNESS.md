# HARNESS 项目笔记

## 技术栈

## 编码约定

## 架构决定

## 已知问题

## 当前进展


## 技术栈（更新）

- 项目方向调整为**纯前端**：只做前端，不做后端。
- 交付形态：**单文件 HTML**，CSS / JS 全部内嵌，不使用外链资源。
- 此前记录的 FastAPI + PostgreSQL 方案**不再适用**（保留为历史记录，不再作为当前方向）。

## 编码约定

- 一个功能 = 一个 HTML 文件，CSS 与 JS 内嵌在同一文件内，按分区加注释。
- 禁止外链图片、字体、CDN，保证双击即可离线打开。
- 默认按移动端优先设计，375px 宽度不横向溢出、文字不遮挡。
- 交互操作必须有可见反馈，控制台不得报错。
