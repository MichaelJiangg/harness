# 版本记录

## V0.10.2 — Add-统一配置管理

Git 标签：`v0.10.2`。

- 运行时配置从 `pyproject.toml` 的 `tool.harness` 迁移到统一配置：支持 `.harness/config.toml`、`HARNESS_*` 环境变量和 CLI 参数，优先级为 CLI > 环境变量 > 配置文件 > 内置默认值。
- 新增 `--config`、`--provider`、`--model`、`--max-turns`、`--context-window`、`--tool-timeout`、`--mcp-server` 和 `--show-config`；缺少配置文件时使用内置默认值正常启动。
- 新增 `engine.max_turns`，模型名称、上下文字符窗口、工具超时和 MCP 列表可分层覆盖；`.harness/mcp.json` 作为兼容层优先读取。
- `pyproject.toml` 现在只保留项目元信息、Python 版本和依赖。

## V0.10.1 — Add-连接所有模块

Git 标签：`v0.10.1`。

- 新增第十部分组装入口 `harness/app.py`：`create_app()` 加载配置、选择 provider 并创建客户端，返回唯一 `start()` 函数；`start()` 统一启用权限、内置与 MCP 工具、记忆、笔记、Hooks、Agent 编排和终端渲染。
- `python -m harness` 改为调用 `create_app()()`，保留 `--help` 无密钥启动行为和配置错误先于密钥加载的约束。

## V0.9.3.1 — bugfix

Git 标签：`v0.9.3.1`。

- 交互终端在模型回答或工具执行期间支持按 `Esc` 中断；仅在真实 `sys.stdin` 上临时启用 cbreak，Esc 会设置取消信号并唤醒待确认工具。中断后会创建新的取消事件和执行器，当前任务收尾后可以继续下一轮问答；管道和测试输入行为不变。
- 修复 Esc／cbreak 之后普通输入无法正常编辑的问题：真实终端空闲输入改用 `input()`＋readline，方向键移动和退格修改恢复正常。
- busy 状态的 cbreak 读取改为轮询 worker 是否结束，模型完成后立即退出 cbreak，避免下一次普通输入继续吃到原始控制字符。
- 普通单行输入改为一次回车发送；手工多行使用第一行末尾 `\` 续行，粘贴多行通过首行后的待读输入自动识别。真实终端将 prompt 原样传给 readline，提示符不会再被退格删除。

## V0.9.3 — Add-多行输入fix

Git 标签：`v0.9.3`。

- 新增本地命令注册表 `harness/commands/`：每个命令一个文件，通过 `@register()` 自动发现；`/help` 动态列出注册命令和既有命令，新增命令不需要修改 `cli.py` 主循环。
- 新增 `/clear`、`/history`、`/model`、`/tools`，并把 `/cost`、`/compact`、`/help` 迁入统一命令分发。
- `/model` 可查看当前模型并在会话中切换 DeepSeek／GLM，切换后查询与后续用量按新 provider 执行；缺少对应 API key 时给出明确错误。`/cost` 对 USD／CNY 等不同币种分别汇总，不跨币种相加。
- 交互终端新增多行输入：回车换行，连续两个空行发送；续行显示行号，粘贴多行文本不会因内部空行提前发送。模型运行期间的确认和本地命令保持单行即时响应，管道协议不变。

## V0.9.2 — Add-新增GLM模型支持

Git 标签：`v0.9.2`。

- 新增 `HARNESS_PROVIDER=auto|deepseek|glm`：默认 `auto` 在 `GLM_API_KEY` 非空时优先 GLM，否则回退 DeepSeek；可强制指定 provider，对应密钥缺失时启动给出明确错误。
- 将 `DeepSeekClient` 重构为 `ChatCompletionClient(api_key, provider=...)`，保留旧名称作为兼容别名；两个 provider 的模型、端点、超时和错误提示按配置区分。
- DeepSeek 继续使用 `deepseek-flash` 并关闭思考模式；GLM 使用 `glm-5.3-flash`、启用思考模式，默认 `reasoning_effort=low`。
- GLM 流式返回的 `prompt_tokens_details.cached_tokens` 归一化为既有账本使用的缓存命中／未命中字段；GLM 官方未核验到明确单价前，配置保留 `0.0 CNY` 占位，token 统计正常、费用按 0 估算。
- Bash、Hooks 和 MCP 子进程同步清理 `GLM_API_KEY`，启动横幅与帮助文案根据当前 provider 动态显示。

## V0.9.1 — Add-进度反馈、速度提升

Git 标签：`v0.9.1`。

- 等待模型响应时显示优雅的旋转动画和「思考中...」提示，收到首个文字后自动切换到流式回答。
- 主查询直接执行的工具现在实时显示工具名、脱敏参数、成功／失败状态和耗时；结果摘要限长，不打印文件正文。
- 引擎新增 `tool_start` 事件，在权限、确认和执行之前发出，并携带与最终工具结果一致的 `call_id`。
- 委托、后台和 Swarm 内部工具继续静默，只进入 `/activity` 活动日志，避免大量内部调用打断主对话。
- MCP 服务器改为后台启动，提示符不再等待 npx 等外部进程；首个提问最多等待 3 秒完成首次连接并刷新工具，超时则先用内置工具继续。

## V0.8 — Add-MCP

Git 标签：`v0.8`。

- 新增 `harness/mcp.py` stdio MCP 客户端：使用 JSON-RPC 2.0 启动外部服务器、执行初始化、发现工具并转发 `tools/call`。
- 外部工具以 `mcp_<server>_<tool>` 合并进当前查询，名称会移除冒号等 DeepSeek 不接受的字符；每个服务器最多 200 个工具，单个请求默认超时 120 秒。
- 修复 `mcp:<server>:<tool>` 工具名导致 DeepSeek HTTP 400 的问题，并保证自动重连后工具名保持稳定。
- 新增统一服务器配置 `.harness/mcp.json`：按服务器名称配置 `command`、`args` 和 `env`，最多 50 台；文件存在时优先于 `pyproject.toml` 的 MCP 回退列表。
- MCP 管理器增加连接状态、工具数量、运行时间、错误和重试次数追踪；每台服务器由独立后台线程监控，异常退出后自动指数退避重连并重新发现工具。
- 新增 `/mcp` 命令，本地查看所有外部服务器状态和工具总数，不调用模型。
- `tool.harness.mcp` 继续提供 `enabled` 和 `servers` 回退配置；服务器名称唯一，命令、参数和环境条目均在启动时校验。
- MCP 外部工具默认中风险询问，支持使用完整名称配置 `allow`／`ask`／`deny`；关闭本地 JSON Schema 子集校验，参数合法性由远端服务器处理。
- MCP `env` 支持 `${VARIABLE}` 引用，从进程环境或项目 `.env` 解析；缺失或为空时跳过对应服务器，不把密钥写入 `pyproject.toml`。
- 默认接入 Tavily 搜索 MCP：`npx -y tavily-mcp@0.2.22`，通过 `${TAVILY_API_KEY}` 注入 `.env` 密钥，提供 search、extract、crawl、map 和 research 工具。
- MCP 子进程默认不继承 DeepSeek/Tavily 密钥和动态加载器环境，并持续排空 stderr；单台服务器失败不影响其他服务器，退出时关闭全部子进程。
- `python3 -m harness` 默认启用 MCP 配置，`run_cli` 嵌入调用默认关闭。

## V0.7.2 — Add-Hooks And Skill

Git 标签：`v0.7.2`。

- 新增技能包系统：技能 JSON 放在 `.harness/skills/`，包含名称、描述、提示词和工具列表。
- 新增 `/skill` 查看可用技能，`/skill <name>` 激活技能，`/skill off` 停用；激活后限制当前查询可用工具并注入技能提示词。
- 新增三个默认启用的预置钩子：会话启动加载项目约定和历史记忆、写入后自动格式化、会话结束自动保存记忆；可在 `tool.harness.presets` 中分别关闭。

## V0.7.1 — Add-钩子机制

Git 标签：`v0.7.1`。

- 新增生命周期 Hooks：支持 `session_start`、`session_end`、`before_send_message`、`after_reply`、`before_tool`、`after_tool`。
- Hooks 从当前工作区 `.harness/hooks.json` 加载，支持 `shell`、`prompt` 和 `python` 三种类型；Python 函数从 `.harness/hook_functions.py` 加载。
- Shell Hook 使用独立超时并清除 DeepSeek/Tavily 密钥环境；错误不会中断主流程，但会在终端和活动日志中显示。
- Hooks 同时应用于主查询、委托、后台分析和 Swarm 角色。

## V0.6.3.1 — Add-智能搜索、记忆注入

Git 标签：`v0.6.3.1`。

- 工具执行、逐请求用量和上下文压缩事件默认移入后台活动日志，不再逐条打印到对话。
- 新增 `/activity [latest|all|clear]`，按需查看工具参数、结果摘要、请求用量、压缩、重试和编排事件。
- 委托、后台任务和 Swarm 保留高层进度显示；权限确认和错误仍直接显示。
- 新增受控 `web_fetch(url)` 工具：只读取公开 http/https URL，提取 HTML、纯文本、JSON 或 XML 正文，拒绝私网、回环、链路本地、非标准端口、重定向、敏感内容和超过 1 MiB 的响应；默认需要用户确认。
- `web_fetch` 不提供搜索，未知产品仍应先向用户索取官方 URL。
- 新增 `web_search(query, max_results=5)`：接入 Tavily Search API，返回标题、URL、摘要、相关度和发布时间；API key 从本地 `.env` 的 `TAVILY_API_KEY` 读取，默认需要用户确认，密钥不进入代码、日志或 GitHub。
- 修复 Swarm 错误分类：只有团队或单角色计数器真正达到上限才返回请求额度错误，`APIError` 和其他运行时异常分别返回 `swarm_role_api_error`、`swarm_role_failed`，不再误报为“团队请求额度耗尽”。
- 新增可选语义记忆召回：安装 ChromaDB 后，把会话摘要写入本地向量库，按当前问题检索相关记忆；未安装时自动退回最近 N 条模式，并提供轻量文本召回。
- 新增 `/recall <query>`：手动按语义或文本相关度搜索历史记忆，支持 `--limit` 和 `--threshold`，结果带相关度评分。
- 记忆摘要增加 `key_points` 字段，用于记录决策、技术细节、问题和解决方案、待办与用户偏好。
- 记忆注入按优先级分层：项目笔记、最近会话、冷记忆搜索；冷记忆优先级最低，容量不足时先裁剪冷记忆，再裁剪较旧的最近会话，项目笔记始终保留。

## V0.6.2 — 优化启动页

Git 标签：`v0.6.2`。

- 启动页品牌调整为 `Delin Harness` 和 `Powered by Deepseek`，不再显示具体模型名称。
- 新增 3 条产品场景示例：Personal Agent 竞品调研、五看三定产品规划、Agent 发布页设计。
- 启动页保留核心能力、5 条常用命令和退出提示；详细权限、工具和命令说明继续放在 `/help`。

## V0.6.1 — 美化样式

Git 标签：`v0.6.1`。

- 终端输出接入 `rich`：AI 回答使用 Markdown 面板和代码语法高亮，系统提示使用灰色，错误使用红色，工具调用展示参数与结构摘要。
- 交互终端输入提示、后台任务、委托、Swarm 和用量信息增加颜色；管道输出继续保留无 ANSI 的兼容文本。
- `pyproject.toml` 增加 `rich>=13.0` 运行依赖；核心查询、工具、记忆和笔记逻辑仍使用标准库。

## V0.6 — Add-记忆系统

Git 标签：`v0.6`。

- 新增本地会话记忆：`python3 -m harness` 正常退出时调用 DeepSeek 提取用户／助手对话中的决定、约束、关键事实、产出和待办，保存为 `.harness/memory.json` 中的一条 JSON 摘要。
- 下次 CLI 启动时自动加载最近 5 条摘要，作为背景上下文注入系统提示；记忆内容只作为项目背景，不替代当前用户指令。
- 新增 `/memory list`、`/memory show <id>`、`/memory delete <id> --yes`、`/memory clear --yes`，本地查看和管理记忆，不调用模型。
- 记忆文件位于当前工作区的 `.harness/`，保持最多 20 条记录和 0600 权限，采用同目录临时文件原子替换；损坏时跳过加载但继续运行。
- 摘要请求只提交用户／助手文字和工具名称，不包含工具结果正文；模型摘要失败时使用最近文字降级保存，请求失败仍按现有账本规则记录。
- 新增记忆存储、启动注入、退出摘要和 CLI 管理回归，全部 534 项离线测试通过；未进行真实 DeepSeek API 联调。
- 新增 `HARNESS.md` 项目长期笔记：启动时读取并注入系统提示，模型通过 `notes_read`、`notes_append`、`notes_replace` 查看、追加和替换项目知识。
- `/notes` 查看笔记，`/notes append <text>` 追加，`/notes replace --yes <text>` 和 `/notes clear --yes` 显式确认后更新；固定工作区根目录单文件，最多 65536 字节，拒绝软链接、目录和二进制内容。
- `notes_append` 默认自动放行，`notes_replace` 默认逐次确认，`deny` 规则始终优先；记忆核心按 `harness/memory/session.py` 与 `harness/memory/injection.py` 拆分，新增笔记存储、工具注册、模型自动记忆、权限和 CLI 回归，全部 547 项离线测试通过；未进行真实 DeepSeek API 联调。

## V0.5 — Add-Agent 编排

Git 标签：`v0.5`。

- 新增自动注册的 `delegate(description, task)`，主 AI 可以派发独立 DeepSeek 子查询，子回答作为工具结果返回，主对话不混入子历史。
- 子任务继承工作区、权限快照、本地确认、会话目录授权和审计；仅获得主查询开放的工具，描述和执行注册表均禁止递归委托。
- 主子请求统一计入 `/cost` 和本次主查询总请求上限，预留一次额度供主 AI 整理；重试、摘要和失败请求继续按原规则记账，取消信号共享。
- CLI 显示委托启动、完成／失败和子任务事件标识，子回答流不直接混入主输出；委托默认放行，可以通过现有权限配置询问或禁止。
- 修复单任务子查询将全部工具历史当作当前轮保留、无法进入摘要的问题；超限时保留原任务与最后一组完整工具结果，摘要更早批次。子上下文或请求额度耗尽后，阻止本次提问再次启动子查询，下一次用户提问重置。
- 根据真实运行反馈，将目录级、多文件、代码质量、跨文件对比等需要大量读取的任务改为优先委托；主会话只有一组工具批次且超限时先扩展下一组完整批次再压缩，完整摘要超过 2000 字符时截断到预算，未完成摘要仍保留原历史。
- 根据再次真实运行反馈，将每次提问的压缩上限从 2 次提高到 6 次，覆盖多文件子查询需要多次阶段摘要的场景；子任务因上下文或请求额度失败时，主 AI 不得转回主会话直接执行原任务。
- 将子任务编排模块由 `harness/agents.py` 重命名为 `harness/orchestration.py`，同步更新运行代码、测试和文档引用。
- 新增会话级后台任务编排：`background_submit` 提交 Bash 命令或独立分析，`background_check` 跨轮查询结果；任务支持排队、最多 5 个并发、默认 300 秒超时和完整状态流转。
- 后台命令仍接受 Bash 禁止规则与确认守卫；后台分析使用隔离对话、继承可用工具、独立请求预算并计入同一用量账本。退出会停止后台任务，结果仅保存在当前进程内存。
- 新增 Swarm 多角色团队协作：角色拥有独立上下文和受限工具集，按 JSON 交接协议传递摘要、产物与打回反馈；默认提供 Coder、Reviewer、Tester 团队，`max_rounds` 防止无限回退。
- 角色工具不得包含委托、后台或再次启动 Swarm 的入口，写文件和 Bash 仍沿用权限确认；CLI 显示 `[swarm]` 角色分配、开始、完成和交接事件。
- 将 Swarm 请求预算与主查询分离：默认团队总预算 120 次、单角色 40 次，主查询保留自己的 20 次额度；单角色或团队预算耗尽时返回明确错误码。
- 扩展保守只读 Bash 白名单，允许安全文件检查命令及版本查询自动执行，同时仍阻止解释器脚本、重定向、`sed -i` 和 `find` 动作等写操作。
- 新增受限验证工具 `run_verify`，仅运行工作区内明确的 Node／Python 脚本，确认展示简短路径与运行方式；Swarm 内部工具、请求、压缩和重试日志默认静默。
- `run_verify` 扩展 `node-test`、`npm-test` 目录验证，并支持当前会话按目录记住验证授权；包含 `bash` 的 Swarm 角色自动获得该工具，后续同一目录测试不再逐次确认。
- 新增提示契约、主查询单批次压缩、超长摘要截断、多阶段压缩、后台管理器、后台工具、后台分析、Swarm 编排、独立预算、扩展只读 Bash 白名单、受限验证和静默编排输出回归，全部 518 项离线测试通过；未进行真实 DeepSeek API 联调。

## V0.5.1 — Add-Auto 模式

Git 标签：`v0.5.1`。

- 新增会话权限模式 `ask`／`auto`，CLI 可通过 `/mode auto [目录]` 信任当前或指定工作目录。
- auto 模式自动放行工作区内读取、写入、验证和常见 Node/Python/Perl 开发命令，仍拦截网络、破坏性、进程管理、敏感路径、越界和重定向操作。
- auto 模式继续允许安全管道、`2>&1`、`2>/dev/null`、`$(pwd)` 和 heredoc 脚本，并让 `run_verify` 在已信任目录内直接执行。
- `deny` 规则始终优先；自动放行操作仍写入权限审计，确认状态标记为 `auto`。
- 新增权限模式、信任目录、危险操作保护和 CLI 切换回归，全部 521 项离线测试通过；未进行真实 DeepSeek API 联调。

## V0.4 — Add-Permission System

Git 标签：`v0.4`。

本版在工具执行前加入可配置权限、风险分级、本地确认、会话目录授权和脱敏审计，保留 V0.3 的查询循环、工具、流式回答、上下文管理、重试和用量统计。

- 新增统一 `PermissionPolicy` 和可配置规则，支持精确工具名、`allow`／`ask`／`deny`、数值优先级、单文件读写目录条件、Bash 命令正则及可选规则 `name`。禁止优先，数值优先级降序，同级按声明顺序，首条匹配生效；没有匹配时按工具配置与风险兜底，项目默认空规则。
- 规则采用不可变启动快照并严格校验，路径与正则检查失败不放行；目录限制覆盖软链接、大小写和 Unicode 别名，执行前复核确认期间的路径变化。目录授权仍不能绕过敏感路径、文件类型和大小限制；`grep` 不提供目录条件，Bash 不接受强制 `allow` 规则。
- 新增 `classify_bash_risk`，检查危险关键词、下载并执行管道、敏感路径、组合命令和环境修改五类信号。仅明确识别的简单 `pwd`／`ls` 及认可参数默认只读放行，未知命令和复杂语法至少确认；`&&`／`||` 简单组合取最高风险。
- 风险提示分三级：读文件、搜索及明确只读命令为低风险；写文件、普通或未知 Bash 为中风险，预览后确认；递归删除、格式化、下载并执行等破坏性命令为高风险，在命令预览前醒目警告。高风险默认确认，永久禁止由 `deny` 决定。
- 权限判断及必要的本地确认在工具执行前完成。拒绝消息告诉模型什么被拒、为什么被拒及安全下一步，不复述敏感正文或原始命令，不允许换工具绕过限制。严格保留 `y` 批准、回车／拒绝／EOF／取消不执行、参数快照和确认期间 `/cost` 可用的行为。
- CLI 写入确认展示完整内容和本会话授权的父目录及子目录；明确批准且写入成功后，当前会话内后续写入该范围自动放行。失败或拒绝不新增授权，新会话清空缓存，禁止规则始终优先，Bash 不记忆。嵌入式执行器只有显式传入 `session_cache=SessionPermissionCache()` 才启用目录授权，确认入口须展示授权范围。
- 新增 `.harness/permission.log` JSONL 审计，保存 UTC 时间、来源、会话／调用标识、工具、风险、匹配规则、决策及确认状态。仅保留路径、必要数值、正文／命令／关键词长度和命令指纹，不记录正文、原始命令、搜索词及未知参数；工具副作用前写入失败即阻止执行。
- 日志目录与文件分别使用 `0700`、`0600`，拒绝软链接、硬链接和特殊文件；`.harness/` 排除 Git，同时纳入内置文件读写与搜索工具的敏感路径保护。
- 只读命令使用受限环境与固定系统 `PATH`，不导入函数或动态加载器设置；其他命令也清除 `DEEPSEEK_API_KEY`、`BASH_ENV`、`ENV`、`BASH_FUNC_*`、`LD_*` 与 `DYLD_*`。
- 新建 `pyproject.toml`，集中管理项目元信息、模型与接口、显示、请求与重试、上下文、工具限制、费率和权限；实际执行、工具说明与 Schema 使用同一份启动配置。Python 最低版本为 3.11，使用标准库 `tomllib`，不增加第三方依赖；API key 仍通过 `.env`／环境变量提供。
- 权限、规则、Bash 风险、会话授权、审计、配置及原有功能的 423 项离线测试通过；审计另通过连续 100 轮并发验证，修复 macOS 首次创建日志竞争。未进行真实 DeepSeek API 联调。启发式不等于完整 Shell 解析或沙箱，路径检查与执行不具备抵御并发外部修改的原子隔离，审计日志也不承诺防篡改。

## V0.3 — Add Tool System

Git 标签：`v0.3`。

本版将占位工具升级为可实际调用的三层工具系统，包含读文件、确认后写文件、确认后执行命令，以及关键词搜索；沿用 V0.2 的流式输出、上下文管理、重试和用量统计。

- 工具统一迁入 `harness/tools/`，分离 Definition、Registry 和 Executor；模块提供 `DEFINITION` 与 `execute` 即可自动发现注册，无需维护工具列表或修改核心循环。
- 使用 `name`、`description`、`input_schema` 定义工具，对接 DeepSeek 时转换为 `function.parameters`；执行层统一校验类型、必填字段和参数范围。
- 实现真实 `read_file(path, offset=0, limit=...)`，按行返回 UTF-8 文件内容，省略 `limit` 时读取剩余行。
- 新增自动注册的 `write_file(path, content)`，终端先展示目标路径、完整内容及新建／覆盖提示，每次明确输入 `y` 后才创建父目录和写入。拒绝、退出、输入关闭或非交互模式均不执行待确认的写入；等待确认时仍可使用 `/cost` 和 `/help`。
- 确认由执行层强制要求，模型不能自行批准；写入沿用工作目录和敏感路径限制，UTF-8 内容最大 1 MiB。同目录临时文件原子替换已有内容，覆盖失败保留原文，错误继续回传模型。
- 批准后重新核对目标路径与新建／覆盖状态，等待期间发生变化时拒绝写入，避免未提示的覆盖。
- 新增自动注册的 `bash(command, timeout=30)`，完整预览命令、工作目录和超时后逐次确认；最多 120 秒，超时或取消清理同组进程。支持 macOS／Linux，标准输入关闭，不读取 Bash 启动脚本，不继承 Harness 的 API key。
- 命令分别回传 stdout、stderr、实际退出码与超时／取消标记，非零退出仍保留输出；已启动命令发生输出读取故障时同样保留结果并标记执行失败。
- 两路命令输出持续排空，每路最多保留 64 KiB 首尾；模型上下文中进一步分别截断并保留退出码，JSON 严格遵守 6000 字符限制。
- 新增自动注册的只读 `grep(keyword, path=".", glob="*", max_results=100)`，按区分大小写的字面关键词搜索，可用 `*.py` 等文件名模式筛选，返回相对路径、从 1 开始的行号和行内容。
- 搜索同时限制条数和结果长度，超长行保留关键词附近片段并标记截断，完整匹配条目不被拆散；跳过不可读、二进制、无效编码、过大或受限文件，报告跳过数量，支持取消，无需用户确认。
- 读取范围固定为会话启动目录及子目录，拒绝越界、敏感路径和非普通文件；单文件最大 1 MiB，返回模型的结果仍按 6000 字符截断。
- 工具错误以代码和说明回传，模型可修正参数继续处理；终端显示简短状态。
- 284 项离线测试通过，覆盖自动发现、统一参数校验、文件读写、关键词及文件类型搜索、行号与截断边界、逐次确认、命令输出与退出码、超时和子进程清理、输出限长，以及真实临时文件与模拟模型的完整调用流程；真实 DeepSeek API 联调尚未验证。

## V0.2 query engine

Git 标签：`v0.2`。

### 功能

- Python 标准库实现 DeepSeek 查询循环，支持多轮对话、多个工具调用及按 `tool_call_id` 回传结果。
- 流式回答边生成边显示，交互终端默认每字符约 20 毫秒，管道输出不限速。
- 上下文超过 24000 字符后自动生成摘要，保留最近 4 个完整用户轮次及当前轮；支持 `/compact` 手动压缩。
- 每次操作最多压缩 2 次，无法缩短到预算内时提示「太长了，建议开个新会话」。
- 工具结果超过 6000 字符时保留首尾并标注截断。
- 暂时性 API 错误最多重试 3 次，依次等待 1、2、4 秒；失败请求、重试和摘要请求统一计入用量记录。
- 每次请求记录 token，使用 `/cost` 查看会话累计用量和 USD 预估费用。
- 自动读取项目根目录 `.env`，环境变量优先，无需每次手动注入密钥。

### 验证与限制

- 101 项离线测试通过，命令行帮助入口通过验证；未进行真实 DeepSeek API 联调。
- `read_file` 和 `run_command` 仍为占位工具，不实际读取文件或执行命令。
- 对话和费用记录仅保存在当前进程内存中，退出后清空。
- 费用为预估值；失败请求可能拿不到用量，实际扣费以 DeepSeek 账单为准。
- 退出会停止后续循环，但不能保证立即中断在途 HTTP 请求或远端生成。
